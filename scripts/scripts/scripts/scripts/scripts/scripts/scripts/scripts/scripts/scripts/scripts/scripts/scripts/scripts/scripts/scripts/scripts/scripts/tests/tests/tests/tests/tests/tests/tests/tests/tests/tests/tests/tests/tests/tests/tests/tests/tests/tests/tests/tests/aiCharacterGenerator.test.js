import { generateAICharacter } from "../scripts/aiCharacterGenerator.js";

test("Should generate AI character based on description", () => {
  const description = "mysterious warrior";
  generateAICharacter(description);
  // Placeholder for assertions related to AI character generation
});
