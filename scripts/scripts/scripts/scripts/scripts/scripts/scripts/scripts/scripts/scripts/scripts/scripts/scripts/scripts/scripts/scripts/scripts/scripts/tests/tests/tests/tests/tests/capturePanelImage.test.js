import { capturePanel } from "../scripts/capturePanelImage.js";

test("Should capture the current manga panel", () => {
  const panelData = { panelId: 1 };
  capturePanel(panelData);
  // Placeholder for assertions related to panel capturing
});
