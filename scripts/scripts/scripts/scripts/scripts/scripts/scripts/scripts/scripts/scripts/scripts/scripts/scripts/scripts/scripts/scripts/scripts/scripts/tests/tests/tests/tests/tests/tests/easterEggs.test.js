import { triggerEasterEgg } from "../scripts/easterEggs.js";

test("Should trigger Easter egg with keyword", () => {
  const keyword = "surprise";
  triggerEasterEgg(keyword);
  // Placeholder for assertions related to Easter egg triggering
});
